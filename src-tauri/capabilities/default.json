{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["openFolder", "setting", "quickInput"],
  "permissions": ["core:default"]
}
